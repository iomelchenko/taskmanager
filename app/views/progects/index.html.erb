<h1>Simple TODO List</h1>

<table class="table table-striped table-bordered table-condensed">
  <tr>
    <th></th>
    <th>Progect_Id</th>
    <th>Name</th>
    <th>Status</th>    
    <th>Deadline</th>
    <th></th>
    <th></th>
    <th></th>

  </tr>

    <% @progects.each do |progect| %>
      <% if progect.user_id == current_user.id %>
      <tr>
        <th><i class="icon-plus"></i></th>
        
        <td><%= progect.id %></td>
        <td><%= progect.name %></td>
        <td></td>
        <td></td>        
        <td><%= link_to "Show #{progect.id} progect", progect %></td>
        <td><%= link_to "Edit #{progect.id} progect", edit_progect_path(progect) %></td>
        <td><%= link_to "Destroy #{progect.id} progect", progect, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>

      </tr>

      <% end %>
            <% progect.tasks.each do |t| %>
               <tr>
                <td></td>
                <td></td>                
                <td><%= "-- Task № #{t.id}: #{t.name}" %></td>
                <td><%= t.status %></td>
                <td></td> 
                <td></td> 
                <td></td>                                                 
                <td></td>                  
              </tr>  

             <% end %>
    <% end %>

</table>

<br />

<%= link_to 'New Progect', new_progect_path %>
